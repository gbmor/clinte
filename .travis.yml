language: rust

rust:
  - stable

os: 
  - linux

dist: xenial

addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - libsqlcipher-dev
      - libsqlcipher0
      - cmake
      - gcc
      - binutils-dev
      - libiberty-dev

script:
    - cargo build --verbose
    - cargo tarpaulin --out Xml
    - bash <(curl -s https://codecov.io/bash)
